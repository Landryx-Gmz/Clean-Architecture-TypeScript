{
  "name": "arquitectura_simple",
  "version": "1.0.0",
  "description": "**Dominio**: Order, Price, SKU, Quantity, eventos de dominio.\r **Application**: casos de uso CreateOrder, AddItemOrder, puertos y DTO.\r **Infra**: repositorio InMemory, pricing estático, event bus no-op.\r **HTTP**: endpoints mínimos con Fstify.\r **Composición**: container.ts como composition root.\r **Tests**: dominio + aceptación de caso de uso.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:migrate": "tsx scripts/migrate.ts",
    "worker:outbox": "tsx src/infrastructure/messaging/OutboxDispatcher.ts",
    "dev": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/main.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "fastify": "^4.29.1",
    "pg": "^8.12.0",
    "pino": "^9.0.0",
    "pino-pretty": "^11.2.2",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.16.0",
    "eslint": "^9.0.0",
    "tsx": "^4.15.7"
  },
  "author": "",
  "license": "ISC"
}